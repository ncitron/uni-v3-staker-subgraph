{
  "name": "uni-staker-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ uni-staker-test",
    "create-local": "graph create --node http://localhost:8020/ uni-staker-test",
    "remove-local": "graph remove --node http://localhost:8020/ uni-staker-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 uni-staker-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
